# Optional: 覆寫短網址服務 endpoint。
# 預設已經寫死在 src/utils/shortener.js（公開 GAS 部署），平常不用填。
# 只有要換到備援部署 / 自架 server 時才設這個。
VITE_GAS_URL=

# Google Analytics Measurement ID (e.g. G-XXXXXXXXXX)
VITE_GA_MEASUREMENT_ID=
